{
  "description": "ACK rejection case where the engine returns a capability the client did not advertise.",
  "context": {
    "client_handshake": "handshake_valid.bin",
    "client_capabilities": "0x000000",
    "engine_returned_capabilities": "0x000001 (CAP_CORRELATION)"
  },
  "packet": {
    "size_bytes": 4,
    "byte_order": "Big-Endian for the 24-bit capability mask",
    "hex": "01000001",
    "fields": [
      { "name": "encoding_selected", "offset": 0, "size": 1, "value": "0x01 (JSON)" },
      { "name": "negotiated_capabilities", "offset": 1, "size": 3, "value": "0x000001 (CAP_CORRELATION)" }
    ]
  },
  "expected": "Reject with ERR_PROTOCOL_VIOLATION (403) and close the connection."
}
